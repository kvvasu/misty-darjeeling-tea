[build]
  command = "pnpm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "24"

# Forms are declared in the HTML (data-netlify="true"); spam filtering is
# enabled at the app level in the Netlify UI (Forms settings).
